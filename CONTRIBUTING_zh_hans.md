# 为 kristal-i18n-example 贡献翻译

感谢你愿意贡献！🌍

## 这个模组是干嘛的：翻译原版模板

**kristal-i18n-example** 是 [kristal-i18n](https://github.com/Bli-AIk/kristal-i18n) 的集成测试与演示模组，内容就是完整的原版 Kristal `v0.10.0` 模板。最麻烦的部分已经全部做完：

- **模板文本都已提取。** 对话、过场、选项、物品、武器、防具、法术、菜单、存档界面、商店、Debug 菜单、战斗结算、光世界/暗世界 UI——英文原文在 `lang/en.json`，翻译参考在 `lang/zh_hans.json`。
- **hook 全部就位。** [kristal-i18n](https://github.com/Bli-AIk/kristal-i18n) 库负责所有 hook——你一行 Lua 都不用写。
- **简体中文就是完整参考。** `lang/zh_hans.json` 的翻译（以[好人汉化组](https://github.com/gm3dr/)的汉化为准）覆盖了整个模板，任何时候你都有个「翻完了应该长什么样」的完整范例。

所以这个模组的贡献侧重点是**模板翻译**：把原版模板的内容翻译成你的语言。贡献者只需要**翻译**——复制任意一份已有的语言文件，把值翻译成你的语言，注册一下——完事儿。玩家马上就能在游戏内的**设置菜单**里切换到你的语言。

我全力支持并欢迎为你的语言提交翻译 Pull Request！这正是我创建这个本地化库的初衷——让每个人都能用自己的母语制作游戏、翻译现有的游戏。

> **本指南的侧重：本演示模组的原版模板内容。** 如果你想翻译的是**框架内置所有文本**（引擎自己显示的全部文字），请看 [kristal-i18n 的贡献指南](https://github.com/Bli-AIk/kristal-i18n/blob/main/CONTRIBUTING_zh_hans.md)。

## 添加一门新语言

1. **挑一份起点文件。** 复制任意一份已有的语言文件，命名为 `lang/<你的语言>.json`（比如法语 `lang/fr.json`、日语 `lang/ja.json`）。`lang/zh_hans.json` 是最完整的参考，也可以从你读起来最顺的语言开始。
2. **翻译所有值。** 把每个值翻译成你的语言。**键名不要动**——游戏按键名查找文本，所有语言共用同一套键。
3. **注册语言。** 在 `mod.json` 的 `config.kristalI18n` 里，把语言 ID 加进 `languages`，把显示名加进 `languageNames`：

   ```json
   "languages": ["en", "zh_hans", "fr"],
   "languageNames": {
       "en": "English",
       "zh_hans": "简体中文",
       "fr": "Français"
   }
   ```

4. **（可选）姓名和资源。** 在 `lang/names.json` 里为你的语言添加角色名；如果语言需要，还可以把贴图/字体/音频覆盖放到 `lang/<你的语言>/...` 路径下（见 README 的特性一节）。
5. **游戏内测试。** 在 Kristal 里启动模组，按切换键（默认 `F7`）随时来回切换语言检查效果。
6. **提交 Pull Request。** 🎉

## 翻译注意事项

- **键名永不变，只翻译值。** 改键名会让所有语言都查不到文本。
- **保留占位符。** `{key}` 内插和 `[var:name]` / `[name:kris]` 标记必须保留。可以按你的语法调整它们在句中的位置，但不能丢掉。
- **贴合语气。** 原版模板文本短而紧凑，是配合打字机节奏的。翻译也要保持简洁，才能在游戏里读起来顺畅。
- **注意语境。** 有些键在多个界面共用。拿不准的时候，看看 `lang/zh_hans.json` 怎么翻的，或者跑一遍模组看实际效果。

## 其他贡献方式

- **报告未翻译文本**——如果游戏里出现了没被翻译的原文，发 Issue，附上截图和复现步骤。
- **改进现有翻译**——措辞、语气、一致性修正都欢迎。
- **建议新演示场景**——让模组覆盖库中还没被展示到的功能。
- **库层面的贡献**——新 hook 或新的内置文本属于 [kristal-i18n](https://github.com/Bli-AIk/kristal-i18n)（见它的贡献指南）。

## 许可证

提交贡献即表示你同意贡献以 MIT License 和 Apache License, Version 2.0 双许可（与项目一致）。
